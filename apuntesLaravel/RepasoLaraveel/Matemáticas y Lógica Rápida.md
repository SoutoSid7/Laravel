// 1. Número aleatorio entre dos valores (ambos incluidos)
$dado = rand(1, 6);

// 2. array_sum(): Suma todos los números de un array.
// Útil para el Blackjack o calcular puntos totales de un historial.
$puntos = [10, 5, -2, 8];
$total = array_sum($puntos); // Devuelve 21

// 3. max() y min(): Sacar el valor más alto o más bajo de un array.
$record = max($ranking);

// 4. Módulo (%): Saber si un número es par o impar.
if ($numero % 2 == 0) {
    // Es PAR
} else {
    // Es IMPAR
}